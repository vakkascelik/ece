{
  "expo": {
    "name": "Doorway",
    "slug": "ece",
    "version": "0.1.0",
    "orientation": "portrait",
    "userInterfaceStyle": "automatic",
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "nz.co.salixtech.ece",
      "infoPlist": {
        "//": [
          "ITSAppUsesNonExemptEncryption declared false, because this app uses only the",
          "HTTPS the platform provides and Apple's own exemption covers that. Declaring",
          "it here answers the export-compliance question once instead of at every",
          "submission, and answering it carelessly under release pressure is how a build",
          "sits in review for a week.",
          "",
          "No NSPhotoLibraryUsageDescription and no NSCameraUsageDescription, because the",
          "mobile app does not take or attach photos — media is uploaded from the web app.",
          "A usage string for a permission the app never requests invites a reviewer to",
          "ask why, and the honest answer would be 'it does not'."
        ],
        "ITSAppUsesNonExemptEncryption": false
      }
    },
    "android": {
      "package": "nz.co.salixtech.ece",
      "//": [
        "No `permissions` array. expo-notifications adds POST_NOTIFICATIONS itself, and",
        "a hand-written list here would drift from what the plugins actually need. There",
        "is no location, camera, microphone, contacts or storage permission in this app."
      ],
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#1b1a18"
      },
      "//adaptiveIcon": [
        "backgroundColor is MARK.box.fill. The box is the OUTER layer here, not drawn into the",
        "foreground, because Android masks the foreground to the launcher's shape and a mark with",
        "its own corners baked in comes out with those corners sliced off."
      ]
    },
    "extra": {
      "//": "One binary serves every centre. The tenant is resolved at sign-in, never baked into the build — which is the whole reason this platform is pooled rather than one deployment per customer.",
      "eas": {
        "projectId": "6df0b4d4-e4d5-4057-b2e6-66eadf431e77"
      }
    },
    "plugins": [
      "expo-sqlite",
      "expo-secure-store",
      "expo-notifications"
    ],
    "//": [
      "Icons are GENERATED from MARK by `npm run icons:mobile` — see scripts/mobile-icons.ts for why",
      "these are committed rasters when the web renders the same geometry at request time.",
      "",
      "STILL MISSING BEFORE A STORE SUBMISSION:",
      "",
      "  • a splash screen via expo-splash-screen — not a blocker for either store, and a decision",
      "    about the first thing a kaiako sees at 7am rather than something to add with an icon",
      "",
      "docs/store-listing.md holds everything else a submission needs, including the Play",
      "Data Safety answers and Apple's privacy questionnaire — both written out there,",
      "because both are declarations about children's data and both are easy to answer",
      "carelessly."
    ],
    "icon": "./assets/icon.png",
    "notification": {
      "icon": "./assets/notification-icon.png",
      "color": "#1b1a18"
    },
    "//notification": [
      "Android draws the ALPHA CHANNEL of this file in white and ignores every colour in it, so the",
      "asset is a silhouette. Supplying the full-colour icon here is how you get a solid white square",
      "in the status bar — the grey-square failure below, one step further along."
    ],
    "owner": "vakkascelik"
  }
}
