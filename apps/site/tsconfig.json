{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"],
      "@ece/core": ["../../packages/core/src/index.ts"],
      "@ece/api/enquiries": ["../../packages/api/src/enquiries.ts"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "// note": [
    "THIS CHANGED, and the previous note said it would have to be justified here when it did.",
    "It used to read: no @ece/api path, this app never talks to Supabase, the public site has",
    "no database access at all. That was true until the careers form, which had to create a",
    "row somewhere. What is still true is the part that mattered: the service-role key stays",
    "in apps/web and never comes here, and `anon` still has no table grant, which",
    "review:security asserts at high severity.",
    "",
    "THE RECRUITMENT PATH IS GONE AGAIN, 2026-08-27. The careers form was removed on the",
    "owner's instruction and applications go by email now, so the site no longer imports",
    "@ece/api/recruitment. One path left, and one reason left for this container to reach",
    "Postgres at all: the enrolment enquiry form. Remove that and this note can go back to",
    "what it originally said.",
    "",
    "The path is deliberately to ONE MODULE and not to @ece/api's index. enquiries.ts has no",
    "runtime imports — both of its imports are types, which erase — so this brings in the one",
    "function the site needs and not createServiceClient, which the public container has no",
    "business being able to construct.",
    "",
    "The browser still reaches nothing: the anon key is read from an unprefixed env var, so",
    "Next cannot inline it into client JS, and the CSP's `connect-src 'self'` stays literally",
    "true. Only the site's server talks to Postgres, and only through one definer function."
  ],
  "exclude": ["node_modules"]
}
